docker-compose {
    delta-uri = "https://test.nexus.bbp.epfl.ch/v1"
    realm = "http://keycloak:8080/auth/realms/nexus-tests"
    realm = ${?TEST_REALM}
    patience = 3 minutes
    patience = ${?TEST_PATIENCE}
}